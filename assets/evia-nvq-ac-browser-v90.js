(()=>{
"use strict";
const VERSION=90,RPL_KEY="evia-rpl-ksbs-v1",EVIDENCE_KEY="evia-selfobs-live-v3";
function ctx(){const c=window.EviaCourseContext?.current?.();return c?.courseType==="nvq"?c:null}
function meta(){return window.EviaTrowelMeta||null}
function esc(s){return String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]))}
function read(k,d){try{const x=JSON.parse(localStorage.getItem(k)||"null");return x??d}catch{return d}}
function numeric(a,b){return String(a).localeCompare(String(b),undefined,{numeric:true,sensitivity:"base"})}
function desc(code){return window.EviaTrowelACText?.describe?.(code)||window.EviaTrowelHandbook?.describe?.(code)||"Assessment criterion"}
function rplSet(){const c=ctx(),allowed=new Set(c?.codes||[]),x=read(RPL_KEY,[]);return new Set((Array.isArray(x)?x:[]).filter(code=>allowed.has(code)))}
function evidenceMap(){const c=ctx(),out=new Map();if(!c)return out;const allowed=new Set(c.codes||[]),xs=read(EVIDENCE_KEY,[]);if(Array.isArray(xs))xs.forEach(e=>(Array.isArray(e?.codes)?e.codes:[]).forEach(code=>{if(allowed.has(code))out.set(code,(out.get(code)||0)+1)}));return out}
function coveredSet(){const out=rplSet(),e=evidenceMap();e.forEach((n,code)=>{if(n>0)out.add(code)});return out}
function routeUnits(){const c=ctx(),m=meta();if(!c||!m)return[];const xs=Array.isArray(c.units)&&c.units.length?c.units:[...new Set((c.codes||[]).map(code=>m.codeUnit?.[code]).filter(Boolean))];return xs.map(Number).filter(Boolean)}
function codesForUnit(unit){const c=ctx(),m=meta();if(!c||!m)return[];const allowed=new Set(c.codes||[]);return (m.unitCodes?.[String(unit)]||[]).filter(code=>allowed.has(code)).slice().sort(numeric)}
function unitTitle(unit){return meta()?.unitTitles?.[String(unit)]||`Unit ${unit}`}
function acLabel(code){const p=String(code).split(".");return p.length>2?`AC ${p.slice(1).join(".")}`:`AC ${code}`}
function loOf(code){return String(code).split(".")[1]||""}
function unitOf(code){return Number(meta()?.codeUnit?.[code]||String(code).split(".")[0]||0)}
function stats(codes){const covered=coveredSet(),rpl=rplSet(),e=evidenceMap();return{total:codes.length,covered:codes.filter(c=>covered.has(c)).length,rpl:codes.filter(c=>rpl.has(c)).length,evidence:codes.filter(c=>(e.get(c)||0)>0).length}}
function close(){document.querySelector(".evia-nvq-ac-browser-layer")?.remove()}
function layer(body,title,back=close){close();document.querySelector(".evia-nvq-layer")?.remove();const el=document.createElement("div");el.className="evia-tools-layer evia-nvq-layer evia-nvq evia-nvq-ac-browser-layer";el.innerHTML=`<section class="evia-tools-screen"><div class="evia-tools-head"><button type="button" data-acb-back>‹ Back</button><b>${esc(title)}</b><span></span></div><div class="evia-tools-body">${body}</div></section>`;document.body.appendChild(el);el.querySelector("[data-acb-back]").onclick=back;return el}
function switcher(active){return `<div class="evia-acb-switch" role="tablist"><button type="button" class="${active==="theme"?"on":""}" data-acb-view="theme">By P / T</button><button type="button" class="${active==="unit"?"on":""}" data-acb-view="unit">By unit</button></div>`}
function statusMarkup(code){const r=rplSet().has(code),n=evidenceMap().get(code)||0;if(!r&&!n)return'<span class="evia-acb-status empty">Not covered</span>';return `<span class="evia-acb-status">${r?'<i class="evia-acb-rpl" title="Recognised prior learning">o</i>':""}${n?`<i class="evia-acb-evidence" title="Evidence items">${n>5?`o × ${n}`:"o".repeat(n)}</i>`:""}</span>`}
function acRow(code){const covered=coveredSet().has(code),r=rplSet().has(code);return `<article class="evia-acb-ac${covered?" covered":""}${r?" rpl":""}"><div class="evia-acb-ac-head"><b>Unit ${unitOf(code)} · ${esc(acLabel(code))}</b>${statusMarkup(code)}</div><p>${esc(desc(code))}</p></article>`}
function overall(){const c=ctx(),s=stats(c?.codes||[]),pct=s.total?Math.round(s.covered/s.total*100):0;return `<div class="evia-nvq-overall"><strong>${pct}%</strong><span>${s.covered} of ${s.total} official ACs covered · ${s.rpl} RPL</span></div>`}
function themeGroups(){const c=ctx(),m=meta(),by={};if(!c||!m)return[];(c.codes||[]).forEach(code=>{const id=m.codeTheme?.[code];if(!id)return;const g=by[id]||(by[id]={id,title:m.themeNames?.[id]||id,codes:[]});g.codes.push(code)});return Object.values(by).map(g=>({...g,codes:g.codes.sort(numeric),...stats(g.codes)})).sort((a,b)=>numeric(a.id,b.id))}
function themeRows(groups){return groups.map(g=>`<button type="button" class="evia-tools-row evia-acb-theme-row" data-acb-theme="${esc(g.id)}"><span><b>${esc(g.id)} · ${esc(g.title)}</b><small>${g.covered}/${g.total} ACs covered${g.rpl?` · ${g.rpl} RPL`:""} · across ${new Set(g.codes.map(unitOf)).size} unit${new Set(g.codes.map(unitOf)).size===1?"":"s"}</small></span><em>${g.rpl?'<i class="evia-acb-rpl">o</i>':""}›</em></button>`).join("")}
function openThemes(){const groups=themeGroups(),practical=groups.filter(g=>g.id.startsWith("P")),theory=groups.filter(g=>g.id.startsWith("T"));const el=layer(`${overall()}${switcher("theme")}<p class="evia-tools-copy evia-acb-intro">P and T themes are the holistic links used by Evia. Open any theme to see every official AC connected to it, grouped by unit with the full handbook wording.</p><h3 class="evia-nvq-heading">Practical themes</h3>${themeRows(practical)}<h3 class="evia-nvq-heading">Theory themes</h3>${themeRows(theory)}<p class="evia-nvq-note">Yellow o = evidence. Purple o = recognised prior learning. One real work activity can cover several relevant ACs.</p>`,`AC coverage`,close);bindHome(el)}
function bindHome(el){el.querySelectorAll("[data-acb-view]").forEach(b=>b.onclick=()=>b.dataset.acbView==="unit"?openUnits():openThemes());el.querySelectorAll("[data-acb-theme]").forEach(b=>b.onclick=()=>openTheme(b.dataset.acbTheme));el.querySelectorAll("[data-acb-unit]").forEach(b=>b.onclick=()=>openUnit(Number(b.dataset.acbUnit)))}
function openTheme(id){const g=themeGroups().find(x=>x.id===id);if(!g)return openThemes();const byUnit={};g.codes.forEach(code=>{const u=unitOf(code);(byUnit[u]||(byUnit[u]=[])).push(code)});const blocks=routeUnits().filter(u=>byUnit[u]?.length).map(u=>{const xs=byUnit[u].sort(numeric),s=stats(xs);return `<section class="evia-acb-unit-block"><div class="evia-acb-unit-head"><span><b>Unit ${u}</b><small>${esc(unitTitle(u))}</small></span><em>${s.covered}/${s.total}</em></div>${xs.map(acRow).join("")}</section>`}).join("");layer(`<p class="evia-tools-kicker">${id.startsWith("P")?"Practical":"Theory"} theme</p><h2>${esc(id)} · ${esc(g.title)}</h2><p class="evia-tools-copy">${g.covered} of ${g.total} linked ACs are covered across ${Object.keys(byUnit).length} units. These are the actual ACs connected to ${esc(id)} in Evia's holistic map.</p>${blocks}`,`${id} ACs`,openThemes)}
function unitRows(){return routeUnits().map(u=>{const codes=codesForUnit(u),s=stats(codes),pct=s.total?Math.round(s.covered/s.total*100):0;return `<button type="button" class="evia-tools-row evia-acb-unit-row" data-acb-unit="${u}"><span><b>Unit ${u}${s.rpl?' <i class="evia-acb-rpl">o</i>':""}</b><small>${esc(unitTitle(u))} · ${s.covered}/${s.total} ACs covered${s.rpl?` · ${s.rpl} RPL`:""}</small></span><em>${pct}% ›</em></button>`}).join("")}
function openUnits(){const el=layer(`${overall()}${switcher("unit")}<p class="evia-tools-copy evia-acb-intro">Every required unit for this learner's route is shown below in qualification order. Open a unit for the complete official AC list and handbook wording.</p>${unitRows()}<p class="evia-nvq-note">This is AC portfolio coverage, not assessor sign-off.</p>`,`AC coverage`,close);bindHome(el)}
function openUnit(unit){const codes=codesForUnit(unit);if(!codes.length)return openUnits();const byLo={};codes.forEach(code=>{const lo=loOf(code);(byLo[lo]||(byLo[lo]=[])).push(code)});const sections=Object.keys(byLo).sort(numeric).map(lo=>`<section class="evia-acb-lo"><h3>LO ${esc(lo)}</h3>${byLo[lo].sort(numeric).map(acRow).join("")}</section>`).join("");const s=stats(codes);layer(`<p class="evia-tools-kicker">Unit ${unit}</p><h2>${esc(unitTitle(unit))}</h2><p class="evia-tools-copy">${s.covered} of ${s.total} official ACs are covered${s.rpl?` · ${s.rpl} recognised through RPL`:""}. All ACs are shown in handbook order.</p>${sections}`,`Unit ${unit}`,openUnits)}
function intercept(e){const c=ctx();if(!c)return;const target=e.target?.closest?.('[data-arch="AC"],[data-arch="KSB"],[data-action="coverage"]');if(!target)return;e.preventDefault();e.stopPropagation();e.stopImmediatePropagation?.();openThemes()}
window.addEventListener("click",intercept,true);
window.addEventListener("evia:rpl-changed",()=>{if(document.querySelector(".evia-nvq-ac-browser-layer"))openThemes()});
window.EviaNvqAcBrowser=Object.freeze({version:VERSION,open:openThemes,openThemes,openUnits});
})();
